name: Java CI with Gradle

on:
  push:
    branches: [ "feature/CI-Pipeline" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build-and-test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'

    - name: Setup Gradle
      uses: gradle/actions/setup-gradle@v4

    - name: Grant execute permission for gradlew
      run: chmod +x gradlew

    - name: Build and Test with Gradle
      run: ./gradlew build jacocoTestReport

    - name: Upload Project Artifacts
      uses: actions/upload-artifact@v4
      with:
        name: project-artifacts
        path: build/

  code-analysis:
    runs-on: ubuntu-latest
    needs: build-and-test
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Download Project Artifacts
      uses: actions/download-artifact@v4
      with:
        name: project-artifacts
        path: build/

    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'

    - name: Grant execute permission for gradlew
      run: chmod +x gradlew

    - name: Analyze with SonarCloud
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
      run: >
        ./gradlew sonar --info

  docker-build:
    runs-on: ubuntu-latest
    needs: code-analysis
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Download Project Artifacts
      uses: actions/download-artifact@v4
      with:
        name: project-artifacts
        path: build/

    - name: Downcase repository name
      run: |
        echo "IMAGE_TAG=ghcr.io/${{ github.repository_repository }}/myapp:latest" | tr '[:upper:]' '[:lower:]' >> $GITHUB_ENV

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Log in to GitHub Container Registry
      uses: docker/login-action@v3
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}

    - name: Build and push Docker image
      uses: docker/build-push-action@v5
      with:
        context: .
        file: ./Dockerfile
        push: true
        tags: ghcr.io/${{ f.lower(github.repository) }}/myapp:latest

    - name: Output Docker Image Link
      run: |
        IMAGE_URL="https://github.com{{ github.repository }}/pkgs/container/myapp"
        echo "### Docker Image Created successfully!" >> $GITHUB_STEP_SUMMARY
        echo "Download docker image:" >> $GITHUB_STEP_SUMMARY
        echo "[Link to docker image]($IMAGE_URL)" >> $GITHUB_STEP_SUMMARY
        echo "Link:"
        echo "$IMAGE_URL"